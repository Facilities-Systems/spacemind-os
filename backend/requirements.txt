# Core API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# AI
anthropic>=0.28.0

# Validation & Settings
pydantic[email]>=2.7.0
pydantic-settings>=2.2.0
email-validator>=2.1.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9
aiosqlite>=0.20.0        # SQLite async (dev fallback)

# Templates
pyyaml>=6.0.1

# Env
python-dotenv>=1.0.1

# HTTP client
httpx>=0.27.0

# Utilities
rich>=13.7.0
tenacity>=8.3.0          # Retry logic
python-ulid>=2.2.0       # Better IDs
slowapi>=0.1.9            # Rate limiting

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
python-multipart>=0.0.9   # Required by FastAPI for OAuth2PasswordRequestForm (login)

# Export
reportlab>=4.2.0

# Phase 2 — Multi-Agent / Vector Memory
chromadb>=0.5.0           # Vector memory store (optional — enable_vector_memory=true)

# Phase 3 — Observability
prometheus-fastapi-instrumentator>=6.1.0
sentry-sdk[fastapi]>=2.0.0

# Phase 4 — Excel export (UFM pattern)
pandas>=2.2.0
openpyxl>=3.1.0

# Dev
pytest>=8.2.0
pytest-asyncio>=0.23.0
